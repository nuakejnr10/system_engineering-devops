#!/bin/bash
# Create content in a temporary file
echo "Best School" > tempfile

# Use this exact filename generation
filename=$'\\*\\\'\''"Best School"\\\'\''\\*$?*****:)'

# Rename with the exact required filename
mv tempfile "$filename"
